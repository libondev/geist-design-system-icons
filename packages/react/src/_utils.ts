const KEYS_TO_COMPARE = ['className', 'style', 'width', 'height', 'onClick']
export const shallowEqual = (prevProps: any, nextProps: any): boolean => KEYS_TO_COMPARE.every(k => prevProps[k] === nextProps[k] || (!prevProps[k] && !nextProps[k]))